# Optional: only needed when jsonschema-backed validation is desired.
# The real executor contains a restricted v0.2.2 structural fallback so
# VERIFY_INPUTS and the pilot do not fail during module import on Python 3.13.
jsonschema>=4.23,<5
cryptography>=42,<46
